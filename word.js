var fs = require("fs");
var Letter = require("./Letter");

// Create Word constructor
var Word = function() {
	this.word = ()
}

// use Letter constructor to create an object representing the current word the user is attempting to guess.

// Define an array of new Letter objects representing the letters of the underlying word.

// Define a function that returns the string representing the word.
// This will call the function on each letter object (the first function defined in letter.js)
// Displays the character or an underscore & concatenate those together.


// Define a function that takes a character as an arguent and call the guess function on each letter object (the second function defiend in letter.js)
